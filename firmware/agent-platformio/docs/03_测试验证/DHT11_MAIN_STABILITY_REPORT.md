# DHT11 主工程稳定性验证报告（Phase 3）

- **日期**：2026-07-16
- **目标**：主工程固件（v1.1.0-dht11-adafruit-integration）完整断电重启后连续运行
  ≥5 分钟 / ≥100 次读取，验证 DHT11 集成的长期稳定性。
- **结论**：**`DHT11_MAIN_INTEGRATION_PASS`**（133/133 有效，0 失败，零堆泄漏，无复位）。

---

## 1. 方法
- 主固件已在 Phase 2 烧录（`.pio/build/nodemcuv2/firmware.bin`，289904 字节）。
- 用户执行**完整 USB 断电重启**（拔线 ≥5s 再插回）。
- 重新检测端口 = **COM6**，连续采集 **330 秒**（主固件每 2.5s 自动读取一次）。
- 事后脚本解析 `logs/03_main_stability/main_stability.log`，统计有效率 / 最大连续成功 /
  堆漂移 / 中途复位。

## 2. 结果

| 指标 | 值 |
|------|-----|
| 捕获读取数（OK+FAIL） | 133 |
| 有效（OK） | 133 |
| 失败（FAIL） | 0 |
| 有效率 | **100.00%** |
| 最大连续成功 | **133** |
| 样本编号范围 | 15 → 147（连续无缺号） |
| free_heap 首/末 | 51360 / 51360（delta=0） |
| free_heap 最小/最大 | 51360 / 51360 |
| 温度范围 | 34.0 – 34.9 °C |
| 湿度范围 | 8.0 – 8.0 % |
| 中途复位（APP_BOOT_OK 重现次数） | 0 |
| 估算运行时长 | ~332 秒 |

> 采集接入时板子已从上电重启开始运行，样本 1–14 滚出缓冲区；捕获窗口内
> sample 15→147 连续 133 次全部 OK、无缺号，说明该窗口零失败。

## 3. 判定标准核对

| 判定项 | 要求 | 实测 | 结果 |
|--------|------|------|------|
| 读取次数 | ≥100 | 133 | ✅ |
| 有效率 | ≥95% | 100.00% | ✅ |
| 最大连续成功 | ≥20 | 133 | ✅ |
| 无异常复位/看门狗 | 是 | reboots=0 | ✅ |
| 无堆泄漏 | delta>-2000 | 0 | ✅ |
| 运行时长 | ≥5 分钟 | ~332s | ✅ |

## 4. 结论

**`DHT11_MAIN_INTEGRATION_PASS`**。DHT11（Adafruit 库 + D1/GPIO5）在主工程中长期
运行完全稳定。**三重门禁（Phase 1 + Phase 2 + Phase 3）全部通过**，Phase 4（工程清理）
已解除阻塞。

## 5. 产物
- 日志：`logs/03_main_stability/main_stability.log`（含 CAPTURE_EXIT）
- 主固件：`.pio/build/nodemcuv2/firmware.bin`（289904 字节）
