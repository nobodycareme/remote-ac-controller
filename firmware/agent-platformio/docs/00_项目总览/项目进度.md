# 项目进度（手机远程控制空调 · NodeMCU ESP8266）

> 更新时间：2026-07-17　·　固件版本：`1.1.4-srun-portal-fix`
> 说明：本表为项目全生命周期的权威进度清单，共 54 项。状态图例：
> ✅ 完成　🔷 进行中（含诚实 BLOCKED）　⏳ 待办　🕓 未来阶段（本期范围外）

| # | 事项 | 状态 | 关键结论 / 证据 |
|---|------|------|-----------------|
| 1 | 开发环境检查（VS Code / Git / Python / PATH / COM） | ✅ | `setup-report.md` |
| 2 | PlatformIO Core 迁移到 `F:\PIO\Core`（纯英文路径） | ✅ | `PLATFORMIO_CORE_DIR=F:\PIO\Core` |
| 3 | PlatformIO IDE 扩展安装 | ✅ | — |
| 4 | PlatformIO Core CLI 加入 PATH | ✅ | `F:\PIO\Core\penv\Scripts\pio.exe` |
| 5 | 依赖离线归档（`F:\PIO\Downloads`） | ✅ | 两斜杠 URI 变通方案（K1） |
| 6 | 平台 / 工具链版本锁定 | ✅ | `espressif8266 4.2.1` 等 |
| 7 | NO_PROXY 直连绕过 MITM 代理方案确立 | ✅ | 避免 `PLATFORMIO_OFFLINE` 挂死（K12） |
| 8 | 标准 PlatformIO 工程结构创建 | ✅ | `platformio.ini` / src / include / lib / test |
| 9 | ESP8266 自检固件（chip id/CPU/flash/heap/reset/LED） | ✅ | `ESP8266_SELF_TEST_OK` |
| 10 | 自检固件编译（`pio run`） | ✅ | `logs/accept_pio_run.log` |
| 11 | 自检固件烧录与串口自检（COM6） | ✅ | `logs/accept_smoke.log` |
| 12 | PowerShell 自动化脚本（detect/build/upload/monitor/smoke） | ✅ | `tools/*.ps1` |
| 13 | `serial_collect.py` 串口采集器（UTF-8） | ✅ | 修复 gbk 崩溃（K6） |
| 14 | USB-UART 芯片检测（CH9102，VID:PID `1A86:55D4`） | ✅ | MAC `<REDACTED_MAC>` |
| 15 | NodeMCU COM 端口动态检测 | ✅ | 运行时检测，不硬编码 |
| 16 | DHT11 初测（GPIO4 + 自定义驱动）失败 | ✅ | 0/12、0/24；`DHT11_TEST_REPORT.md` |
| 17 | DHT11 短杜邦线 A/B 对照复测（排除线材） | ✅ | `NO_CHANGE`（K16） |
| 18 | DHT11 软件修复尝试（上拉/时序/中断，R1–R5） | ✅ | 全部失败（改错对象） |
| 19 | XHT11 替换 + GPIO4 自定义驱动测试 | ✅ | 仍失败，同点卡高；`XHT11_TEST_REPORT.md` |
| 20 | 旧测试独立性审查 + `rawTrace` 代码审查 | ✅ | 发现临界区 `Serial.print` 缺陷 |
| 21 | XHT11 @ D1/GPIO5 + Adafruit 标准库最小测试 | ✅ | **20/20 PASS**，推翻“供电故障”假设 |
| 22 | 根因归因 | ✅ | 旧故障主要问题是自定义 dht_service/rawTrace 测试方法缺陷；D2/GPIO4 是否有独立异常未用 Adafruit 单变量验证；当前正式方案 Adafruit DHT 1.4.7 + D1/GPIO5 |
| 23 | 厂家红外资料审查（协议 / 帧结构 / 校验和） | ✅ | `F:\PIO\红外资料` |
| 24 | IR 协议分析文档 | ✅ | `04_红外模块/IR_PROTOCOL_ANALYSIS.md` |
| 25 | ZJ-IR-V2 只读 UART probe | ✅ | `IR_UART_PASS baud=115200`，地址 0x00 |
| 26 | IR learn / emit | ⏳ | 待用户授权，本轮未执行 |
| 27 | 阶段1：DHT11 @ D1/GPIO5 Adafruit 独立最小化验证 | ✅ | **DHT11_GPIO5_MINIMAL_PASS**（30/30，100%） |
| 28 | 阶段2：DHT11 合并到主工程（三层引脚架构 + 传感器模块） | ✅ | `sensors/dht11_sensor.*` + `hardware_config.h` |
| 29 | 阶段2：主工程编译 / 烧录 / 启动标记 / 周期读取 | ✅ | `firmware.bin` 289904 B，heap 恒定 |
| 30 | 阶段3：主工程稳定性验证（≥5min / ≥100 次） | ✅ | **DHT11_MAIN_INTEGRATION_PASS**（133/133） |
| 31 | 三重门禁（阶段1 + 阶段2 + 阶段3）全部通过 | ✅ | 清理解锁 |
| 32 | 阶段4：工程清理（旧驱动/测试/临时环境/缓存） | ✅ | **CLEANUP_PASS**，干净重建成功 |
| 33 | 阶段5：项目文档重构与分类（00–07） | ✅ | 本文档所在结构 |
| 34 | 阶段6+7：ZIP 审查包制作与解压复验 | ✅ | 已完成（v0.3.1，REVIEW_PACKAGE_PASS） |
| 35 | Wi-Fi / 校园网接入（代码+运行时验证） | ✅ | 关联/门户/srun 客户端/CLI 已实现并实证；live 登录与稳定性测试待真实凭据 |
| 36 | 校园网 Wi-Fi 关联（OPEN `stu-xdwlan` + DHCP） | ✅ | `WIFI_ASSOC_PASS`，`LOCAL_IP=10.0.x.x`，`DNS=198.51.100.53` |
| 37 | 校园透明门户检测（srun，`ac_id=8`） | ✅ | `PORTAL_PROBE` 与 CLI `net check` 均 `CAPTIVE_PORTAL_DETECTED host=w.xidian.edu.cn` |
| 38 | 校园网认证客户端（**vendored srun-c v1.1.0 硬替换自制算法**） | ✅ | 7 文件逐字节一致 + 1 偏离(TLS 固定)；无凭据→`AUTH_BLOCKED_NEEDS_LOCAL_CREDENTIALS` |
| 39 | `WifiManager` 状态机 + CLI（`wifi`/`net`/`campus`） | ✅ | 受 `ENABLE_WIFI` 保护；主固件 CLI 实测响应正确 |
| 40 | 主固件合并 Wi-Fi+校园网（编译/烧录/CLI 验证） | ✅ | `nodemcuv2` SUCCESS 422,656 B；boot OK + 关联 + 门户检测 `CAPTIVE_PORTAL_DETECTED=YES` + `AUTH_BLOCKED` 全链路实证 |
| 41 | 真实校园网登录（live srun login + 联网验证） | 🔷 | `AUTH_BLOCKED`：仓库无 `secrets.h`，未发起任何真实登录（诚实阻断，非失败） |
| 42 | ≥60min 联合稳定性测试（DHT+IR+Wi-Fi+auth） | 🔷 | 凭据门控：无 `secrets.h` → 仅报告 `AUTH_BLOCKED`，不执行真实认证稳定性 |
| 43 | IR learn / emit | ⏳ | 待用户授权，本轮未执行 |
| 44 | srun-c 供应商钉固（commit `1881da8`，v1.1.0，WTFPL） | ✅ | `docs/03_协议与接口/第三方_srun-c_来源与版本.md` + `_srun_sha256_manifest.json` |
| 45 | 算法一致性校验（Phase 6） | ✅ | `SRUN_VENDOR_SHA_PASS` / `SRUN_ALGORITHM_VECTOR_PASS` / `OLD_AUTH_IMPLEMENTATION_EXCLUDED` |
| 46 | 旧自制实现弃用与隔离 | ✅ | 移入 `archive/rejected_auth_implementation/`，`OLD_AUTH_IMPLEMENTATION_EXCLUDED` PASS |
| 47 | 凭据加载修正（`__has_include("secrets.h")` 替换 `#ifdef SECRETS_H`） | ✅ | 无凭据 `CAMPUS_CREDS_READY=NO`，启动不打印账号/密码 |
| 48 | TLS 证书固定（替换 `setInsecure()`） | ✅ | `setFingerprint(CAMPUS_CERT_SHA1)`，指纹不匹配拒绝连接且不发凭据 |
| 49 | 校园参数再取证（Phase 7） | ✅ | `docs/03_协议与接口/校园网参数实证.md`：SSID/host/base_url/ac_id/domain |
| 50 | 独立认证环境门禁 Gate A/B（Phase 8） | ✅ | `nodemcuv2_campus_auth` 实测 `CAMPUS_CREDS_READY=NO`→`AUTH_BLOCKED_NEEDS_LOCAL_CREDENTIALS` |
| 51 | 六环境 clean build 全 PASS（Phase 14） | ✅ | `nodemcuv2`/`nodemcuv2_probe`/`nodemcuv2_wifi_assoc`/`nodemcuv2_portal_probe`/`nodemcuv2_campus_auth`/`nodemcuv2_srun_c_vector` |
| 52 | 敏感信息扫描（Phase 16） | ✅ | `SENSITIVE_SCAN_PASS`（无真实凭据 / 凭证路径无 `setInsecure()` / 无私钥） |
| 53 | v0.3.3 审查包（独立重建 + 敏感扫描） | ✅ | `Remote_AC_Controller_Review_Package_v0.3.3_*`；从 ZIP 独立重建 `nodemcuv2` SUCCESS |
| 54 | 最终验收判定（Phase 17/18） | ✅ | `READY_FOR_CONTROLLED_LIVE_AUTH`：十二门禁全 PASS / 诚实 `AUTH_BLOCKED`（仅真实登录与联网验证因仓库无 `secrets.h` 未执行） |

## 里程碑小结
- **温湿度链路**：已用广泛验证的 Adafruit 库在 **D1/GPIO5** 上跑通，独立(30/30) +
  主工程(133/133) 双重验证稳定，堆零泄漏、无复位。
- **红外链路**：协议与只读探测已完成（115200 8N1，帧头 `0x68`/帧尾 `0x16`），
  learn/emit 待授权。
- **校园网认证（srun-c 硬替换）**：自制算法已被 vendored `srun-c` v1.1.0 完整替换
  （7 文件逐字节一致 + 1 偏离 TLS 固定），旧实现归档且隔离；凭据/TLS/参数全部修正，
  算法一致性、敏感扫描、四环境 clean build 均 PASS。真实登录**诚实阻断**（`AUTH_BLOCKED`，
  无 `secrets.h`），编译通过 ≠ 认证成功。
- **工程质量**：`platformio.ini` 保留 6 个环境（`nodemcuv2` 生产 / `nodemcuv2_probe` 红外只读 /
  `nodemcuv2_wifi_assoc` 关联 / `nodemcuv2_portal_probe` 门户探测 /
  `nodemcuv2_campus_auth` 独立认证 / `nodemcuv2_srun_c_vector` srun C 向量），均可从零干净重建。
- **未启动**：真实校园网登录与 ≥60min 联合稳定性（凭据门控）、MQTT / 云 / 手机端页面均属后续阶段。
