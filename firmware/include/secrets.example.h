#pragma once
/*
 * Template for the real, git-ignored secrets.h.
 *
 * Copy this file to include/secrets.h and replace the placeholders with the
 * REAL Xidian campus account. Include ONLY the bare campus username (no
 * operator suffix) and the real password.
 *
 *   - username: your campus account, e.g. "2021xxxxxx"  (NO @lt / @yd / @dx)
 *   - password: your campus password
 *
 * include/secrets.h is git-ignored and must NEVER be committed, logged, or
 * packaged into the review ZIP.
 */
#define CAMPUS_USERNAME "YOUR_CAMPUS_ACCOUNT"
#define CAMPUS_PASSWORD "YOUR_CAMPUS_PASSWORD"
