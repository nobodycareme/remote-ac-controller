**简体中文** | [English](../English/first-time-setup.md)

# 首次配置指南

本指南介绍三种常见的首次配置方式：普通家庭/实验室 WPA/WPA2 WiFi、西电 Srun 校园网、以及不配置任何真实凭据的公开构建。所有真实凭据（WiFi 密码、校园账号密码）只存放在本地文件中，仓库内只提交 `.example.h` 模板。

固件的功能开关统一由 [`firmware/shared/RemoteACCore/src/config/feature_gates.h`](../../firmware/shared/RemoteACCore/src/config/feature_gates.h) 控制，各开关组合在该头文件中以编译期 `#error` 形式强制校验。

---

## A. 普通家庭/实验室 WPA 或 WPA2 WiFi

> 适用场景：家庭或办公室 WiFi 为 WPA/WPA2 加密（密码至少 8 位），不需要 Portal 认证。

### A.1 准备本地凭据

```powershell
cd firmware/shared/RemoteACCore/src/config
cp wifi_secrets.example.h wifi_secrets.h
```

编辑 `wifi_secrets.h`：

```cpp
#define LOCAL_WIFI_SSID     "你的WiFi名称"
#define LOCAL_WIFI_PASSWORD "你的WiFi密码"
```

> `wifi_secrets.h` 只保存在本地并已被 Git 忽略，不要将真实密码写进 README、platformio.ini 或提交记录。

### A.2 选择 PlatformIO 编译 Profile

`dev.ps1` 提供两个本地 WiFi Profile：

| Profile | 含义 | 关键开关 |
|---|---|---|
| `local-wifi` | 家庭/实验室 WiFi，无云端 | `ENABLE_CLOUD=0` |
| `local-wifi-cloud` | 家庭/实验室 WiFi + 云端（MQTT） | `ENABLE_CLOUD=1` `ENABLE_CLOUD_CREDENTIALS=1` |

**local-wifi（只连接本地 WPA/WPA2 WiFi，不启用云端）**：

```powershell
cd firmware/agent-platformio
./tools/dev.ps1 build -Profile local-wifi
```

**local-wifi-cloud（需要两个本地配置文件）**：

1. 复制 `wifi_secrets.example.h` 为 `wifi_secrets.h`，填入你自己的路由器 SSID 和密码；
2. 在 `firmware/shared/RemoteACCore/src/config/` 中复制 `cloud_secrets.example.h` 为 `cloud_secrets.h`，填入你自己的 MQTT Broker 配置（主机、端口、用户名、密码、设备 ID、CA 证书或指纹）；PlatformIO 与 Arduino IDE 共用这一份 canonical 本地配置；
3. 执行：

```powershell
./tools/dev.ps1 build -Profile local-wifi-cloud
```

该 Profile 会启用 `ENABLE_CLOUD_CREDENTIALS=1`：缺少 `wifi_secrets.h` 或 canonical `firmware/shared/RemoteACCore/src/config/cloud_secrets.h` 中任意一个时，构建会直接停止并提示缺失文件与模板路径，不会回退为无凭据示例。两个真实文件都已被 Git 忽略，不会提交。旧路径 `firmware/agent-platformio/include/cloud_secrets.h` 与 `firmware/shared/RemoteACCore/src/cloud_secrets.h` 已弃用；任一路径仍存在都会触发硬失败，不能保留多份 Cloud 秘密文件。

> **v1.2.4 起，只复制模板不再能通过。** `local-wifi` / `local-wifi-cloud` 构建前会做内容校验：WiFi SSID 不能是 `your_wifi_name`，密码必须符合 WPA/WPA2 规则（8-63 位可打印字符或 64 位十六进制 PSK）；`local-wifi-cloud` 还会校验 Cloud 配置——Broker 主机不能是 `your-broker.example.com` 等模板值、端口必须在 1-65535、设备 ID 已修改、账号密码已修改、并且至少配置一个有效的 CA 证书或 TLS 指纹。校验失败时构建停止并输出非敏感错误码（如 `HOST_PLACEHOLDER`、`TLS_MATERIAL_MISSING`），不会打印任何秘密值。

> **v1.2.5 起，SSID 与 TLS 规则如下。** WiFi SSID 允许包含普通空格（例如 `Home WiFi`、`Lab Network 2`），长度按 UTF-8 字节计算、上限 32 字节；不能全部由空格组成、不能包含控制字符（0x01-0x1F、0x7F），也不会被自动去除首尾空格或静默截断。TLS 方面：**CA 证书优先**——同时配置有效 CA 与有效指纹时只使用 CA（`setTrustAnchors`）；没有有效 CA 时才使用 SHA-1 服务器证书指纹（`setFingerprint`，40 位十六进制字符，可用冒号分隔），指纹只校验当前服务器证书，证书更新后必须同步更新指纹；两者都没有时构建/初始化停止（`TLS_MATERIAL_MISSING`）。不要关闭 TLS 校验（禁止 `setInsecure()`）。

### A.3 Arduino IDE 模式

在 Arduino IDE 中打开 `firmware/arduino-ide/Remote_AC_Controller/Remote_AC_Controller.ino`，先把模板复制为本地配置文件（由 Arduino ESP8266 构建过程自动应用，不需要修改主 `.ino` 文件）：

```powershell
cd firmware/arduino-ide/Remote_AC_Controller
Copy-Item Remote_AC_Controller.ino.globals.example.h Remote_AC_Controller.ino.globals.h
```

然后编辑 `Remote_AC_Controller.ino.globals.h`：

```cpp
#define ENABLE_WIFI              1
#define ENABLE_WIFI_CREDENTIALS  1   // 启用本地 WPA 凭据
#define ENABLE_AUTO_WIFI_CONNECT 1   // 上电自动连接

// 普通 WiFi 凭据（与 PlatformIO 路径相同）
#include "config/wifi_secrets.h"
```

> 通用 Arduino IDE 操作见 [Arduino IDE 使用指南](Arduino-IDE使用指南.md)。

### A.4 运行时串口命令

固件上电后会用编译进二进制的本地凭据连接家庭 WiFi（`local-wifi` / `local-wifi-cloud`）。如需手动连接或临时切换到开放 SSID，可在串口 monitor 中执行：

| 命令 | 行为 |
|---|---|
| `wifi connect` | 重新选择 `wifi_secrets.h` 中的本地 WPA/WPA2 配置（密码不入串口） |
| `wifi connect <ssid>` | 临时切换到指定的开放 SSID；**不会**读取或使用 `wifi_secrets.h` 中的密码，也不支持在命令行输入 WiFi 密码。SSID 可以是包含空格的名字，例如 `wifi connect Home WiFi` 会把完整的 `Home WiFi` 作为开放 SSID 连接（不会拆分成 `Home`），也不会使用本地 WPA 密码 |
| `wifi status` | 打印连接状态：状态、连接来源（`NET_SOURCE`）、实际 SSID（`NET_SSID`）、IP |
| `wifi scan` | 列出附近 AP（只读） |
| `wifi disconnect` | 断开当前关联 |

连接来源（`NET_SOURCE`）只会是以下之一：`COMPILED_LOCAL_WPA`（本地 WPA 凭据）、`CAMPUS_PROFILE_OPEN`（校园开放 SSID）、`RUNTIME_OPEN_SSID`（`wifi connect <ssid>` 显式指定）、`NONE`（未配置）。`wifi status` 显示的 SSID 与实际连接使用的 SSID 始终一致；开机日志也打印实际 SSID 与来源。

串口日志中不会输出密码内容，只输出：

```
WIFI_CONNECT source=COMPILED_LOCAL_WPA ssid=<ssid> security=WPA_OR_WPA2
WIFI_CONNECT source=CAMPUS_PROFILE_OPEN ssid=<ssid> security=OPEN
WIFI_CONNECT source=RUNTIME_OPEN_SSID ssid=<ssid> security=OPEN
WIFI_CONNECT_SKIPPED source=<source> reason=<SSID_NOT_CONFIGURED|WIFI_PASSWORD_NOT_CONFIGURED|SSID_INVALID|SSID_TOO_LONG>
```

SSID 校验规则（v1.2.5 起，与构建期校验一致）：

- SSID 允许包含普通空格，例如 `Home WiFi`、`Lab Network 2`、`My Router` 都是合法的；
- 长度按 UTF-8 编码后的字节数计算，必须为 1 至 32 字节；超过 32 字节会被拒绝（`SSID_TOO_LONG`），不会被静默截断；
- 不能为空、不能全部由空格组成、不能包含 ASCII 控制字符（0x01-0x1F、0x7F，含 Tab 和换行）；
- 模板值 `your_wifi_name` 会被拒绝；
- SSID 不会被自动去除首尾空格或做任何修改，连接使用的就是输入的原样值。

---

## B. 西电 Srun 校园网

> 适用场景：西电校园 WiFi `stu-xdwlan`（开放 SSID，无 WPA 密码），需要 Portal 认证。
> 注意：西电 Srun 仅作为公开示例 Profile（`profiles/xidian.example.h`）。其他 Srun 部署可参考 [Srun 校园网移植指南](Srun校园网移植指南.md)。

### B.1 PlatformIO 公开示例

```powershell
cd firmware/agent-platformio
./tools/dev.ps1 build -Profile local-campus-example
```

`local-campus-example` 只用于安全的公开编译验证：它编译校园认证代码，验证其能够构建，但**不读取真实校园账号密码、不会执行真实登录**。该 Profile 将 `ENABLE_AUTO_CAMPUS_AUTH=0` 与 `ENABLE_CONTROLLED_LIVE_AUTH=0`，因此不会在开机时自动认证，也不接受真实凭据。

### B.2 真实校园认证（Arduino IDE）

目前公开仓库中可验证的真实配置入口是 Arduino IDE 的本地 globals 配置。先复制模板：

```powershell
cd firmware/arduino-ide/Remote_AC_Controller
Copy-Item Remote_AC_Controller.ino.globals.example.h Remote_AC_Controller.ino.globals.h
```

然后编辑 `Remote_AC_Controller.ino.globals.h`（由 Arduino ESP8266 构建过程自动应用，不需要修改主 `.ino` 文件）：

```cpp
#define ENABLE_WIFI                 1
#define ENABLE_CAMPUS_AUTH          1
#define ENABLE_AUTO_WIFI_CONNECT    1
#define ENABLE_AUTO_CAMPUS_AUTH     1
#define ENABLE_CONTROLLED_LIVE_AUTH 1
#define CAMPUS_PROFILE_HEADER       "profiles/xidian.h"
```

并使用两个本地文件（均被 Git 忽略，不会提交）：

```powershell
cd firmware/shared/RemoteACCore/src/config
cp profiles/xidian.example.h profiles/xidian.h
cp campus_secrets.example.h campus_secrets.h
```

编辑 `campus_secrets.h`：

```cpp
#define CAMPUS_USERNAME "你的学号"
#define CAMPUS_PASSWORD "你的校园网密码"
```

> `xidian.h` 与 `campus_secrets.h` 均不会被提交；不要在 `platformio.ini` 或 `Remote_AC_Controller.ino.globals.h` 中写明文密码。

安全要求：

- 不要关闭 TLS 校验（不使用 `setInsecure()`）；
- TLS 指纹不匹配时必须停止认证，`BAD_CREDENTIALS` / `WRONG_DOMAIN` / `TLS_PIN_MISMATCH` 会进入 `BLOCKED` 状态，不会自动高频重试；
- 每小时配额、最小间隔、失败退避与硬阻断闩锁由 `campus_auth_policy.h` 统一控制。

完整细节见 [西电校园网自动认证](西电校园网自动认证.md)。

---

## C. 无凭据公开构建

> 适用场景：只想编译一次验证公开固件能否安全构建，不需要任何真实凭据。

**public（完整公开固件编译检查）**：

```powershell
cd firmware/agent-platformio
./tools/dev.ps1 build -Profile public
```

`public` Profile 会编译 WiFi 和云端模块，但：

- 不包含任何真实凭据（`wifi_secrets.h`、`cloud_secrets.h` 均不参与）；
- 不会在开机时自动关联 WiFi（没有自动连接配置）；
- 不连接真实 MQTT；
- 适合公开 CI 和工具链验证。

这不是完全离线构建，也不是"只编译传感器"：固件仍编译 WiFi 与云端代码路径，只是不携带任何真实凭据，也不会自动联网。

**public-cloud-example（Cloud 模块编译示例）**：

```powershell
./tools/dev.ps1 build -Profile public-cloud-example
```

该 Profile 仅用于验证无凭据 Cloud 代码能够安全构建：编译 Cloud 模块，但 `ENABLE_CLOUD_CREDENTIALS=0`，不连接真实 MQTT，也不自动关联 WiFi。
